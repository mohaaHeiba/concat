# Simple Contact Manager (C++)

This is a basic C++ console application that lets you manage your contacts. You can add, search, and delete contacts using a simple menu.

## Features
- Add new contacts (name, phone number, email)
- Search for contacts by name
- Delete contacts by name
- Uses a stack structure with linked list implementation
